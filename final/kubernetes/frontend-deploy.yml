apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-frontend-deploy
spec:
  selector:
    matchLabels:
      name: myapp
      tier: frontend
  template:
    metadata:
      labels:
        name: myapp
        tier: frontend
    spec:
      containers:
      - name: nginx
        image: nginx
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"